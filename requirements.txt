# Python packages for server/bridge.py, server/mcp_server.py and
# server/loadtest.py:  pip install -r requirements.txt
# Pinned to the versions this was built and tested with -- mcp's server API
# in particular changes between major versions.
aiohttp==3.13.5
beautifulsoup4==4.14.3
matplotlib==3.11.2
mcp==2.2.0
python-dotenv==1.2.2
requests==2.33.1
starlette==1.6.0
uvicorn==0.53.0
